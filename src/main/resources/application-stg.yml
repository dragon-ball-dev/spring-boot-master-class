spring:
  datasource:
    user:
      jdbc-url: ${STG_USER_DB_URL}
      username: ${STG_USER_DB_USERNAME}
      password: ${STG_USER_DB_PASSWORD}
    product:
      jdbc-url: ${STG_PRODUCT_DB_URL}
      username: ${STG_PRODUCT_DB_USERNAME}
      password: ${STG_PRODUCT_DB_PASSWORD}
server:
  port: ${STG_SERVER_PORT:9090}
