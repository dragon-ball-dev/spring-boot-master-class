spring:
  datasource:
    user:
      jdbc-url: ${PROD_USER_DB_URL}
      username: ${PROD_USER_DB_USERNAME}
      password: ${PROD_USER_DB_PASSWORD}
    product:
      jdbc-url: ${PROD_PRODUCT_DB_URL}
      username: ${PROD_PRODUCT_DB_USERNAME}
      password: ${PROD_PRODUCT_DB_PASSWORD}
server:
  port: ${PROD_SERVER_PORT:9090}
